# Anthropic (research synthesis, qualification, email drafting)
ANTHROPIC_API_KEY=

# Web search for prospecting (Serper.dev - https://serper.dev)
SERPER_API_KEY=

# SMTP for sending outreach emails
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=LogBATT
SMTP_FROM_EMAIL=

# Comma-separated emails that get BCC'd on every send, for visibility
SMTP_BCC=
